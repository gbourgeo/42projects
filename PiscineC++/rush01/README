DONE:
les interfaces module et display.
une methode qui recupere les modules au runtime.

Pour creer un module:
fichier .cpp dans srcs/modules/
fichier .hpp dans includes/
Makefile -> .cpp dans directive LIB =
Suivre l'inerface des modules.

TODO:

trouver comment recuperer ce que contiennent les modules.
creer laffichage ncurses et Qt.
